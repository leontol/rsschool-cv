## ***Anatol Leanovich***
 
## Contacts
* **Github**: leontol
* **Discord**: leontol

## About me
I am 43 years old. I strive to constantly discover something new for myself. Currently, I am actively working on Java Script, HTML and CSS, and I am also learning English at the same time. I constantly hone and improve my knowledge and skills in the field of development. In the future I see myself as a Frontend developer.
## Skills

* HTML
* CSS
* JS
* Git
* C++
## Code

```
    cout << "Решение" << endl;
    double I = floor(((a + b + c) / n) * 1000) / 1000;                 // округляем до 0.001
    cout << "Среднее арифметическое значение " << I << endl;
    double S = floor((sqrt((1 /( n - 1)) * ((pow((a - I), 2) + pow((b - I), 2) + pow((c - I), 2))))) * 1000) / 1000;    // округляем до 0.001
    cout << "Среднее квадратичное отклонение " << S << endl;
    IiU = S;
    double SIiU = floor((IiU) * 1000) / 1000;                        // округляем до 0.001
    cout << "Стандартная неопределённость IiU " << SIiU << endl;
    dIi = 1.5 + (3 * D);                               // Погрешность прибора от диапазона
    cout << "Интервал dIi " << dIi << endl;
    dIiU = dIi / sqrt(3);
    double SdIiU = floor((dIi / sqrt(3)) * 1000) / 1000;             // округляем до 0.001
    cout << "Стандартная неопределённость dIiU " << SdIiU << endl;
    dIit = ((fabs(20 - t)) / 10) * dIi;                // Температура окружающего воздуха
    cout << "Интервал dIit " << dIit << endl;
    dIitU = dIit / sqrt(3);
    double SdIitU = floor((dIit / sqrt(3)) * 1000) / 1000;             // округляем до 0.001
    cout << "Стандартная неопределённость dIitU " << SdIitU << endl;
    dIitt = 1.5 + k +(3 * D);                         // Погрешность ТТ
    cout << "Интервал dIitt " << dIitt << endl;
    dIittU = dIitt / sqrt(3);
    double SdIittU = floor((dIitt / sqrt(3)) * 1000) / 1000;           // округляем до 0.001
    cout << "Стандартная неопределённость dIittU " << SdIittU << endl;
    double CIU = floor(sqrt((pow((IiU), 2)) + (pow((dIiU), 2)) + (pow((dIitU), 2)) + (pow((dIittU), 2))) * 1000) / 1000;    // округляем до 0.001;
    cout << "Суммарная неопределённость " << CIU << endl;
    double Ir = floor((kk * CIU) * 1000) / 1000;    // округляем до 0.001;;
    cout << "Расширенная неопределённость " << Ir << endl;

    cout << "Вклад неопределённости в %" << endl;
    double UIi = floor(((pow((IiU), 2)) / ((pow((IiU), 2)) + (pow((dIiU), 2)) + (pow((dIitU), 2)) + (pow((dIittU), 2)))) * 100 * 1000) / 1000;
    cout << "UIi,% = " << UIi << endl;
    double UdIi = floor(((pow((dIiU), 2)) / ((pow((IiU), 2)) + (pow((dIiU), 2)) + (pow((dIitU), 2)) + (pow((dIittU), 2)))) * 100 * 1000) / 1000;
    cout << "UdIi,% = " << UdIi << endl;
    double UdIit = floor(((pow((dIitU), 2)) / ((pow((IiU), 2)) + (pow((dIiU), 2)) + (pow((dIitU), 2)) + (pow((dIittU), 2)))) * 100 *1000) / 1000;
    cout << "UdIit,% = " << UdIit << endl;
    double UdIitt = floor(((pow((dIittU), 2)) / ((pow((IiU), 2)) + (pow((dIiU), 2)) + (pow((dIitU), 2)) + (pow((dIittU), 2)))) * 100 * 1000) / 1000;
    cout << "UdIitt,% = " << UdIitt << endl;
    

```

## Experience
*I have minimal practical experience in layout. I have no commercial experience.*

## Languages
* Russia  (native)
* Belarusian  (native)
* English (beginner, in the process of learning)
